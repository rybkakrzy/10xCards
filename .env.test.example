# Test Environment Variables
# Copy this file to .env.test and fill in your test values

# Supabase Configuration (use local instance for testing)
PUBLIC_SUPABASE_URL=http://localhost:54321
PUBLIC_SUPABASE_ANON_KEY=your-local-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-local-supabase-service-role-key

# OpenRouter API Key (for AI features)
OPENROUTER_API_KEY=your-openrouter-test-key

# Test User Credentials
E2E_USERNAME=test@example.com
E2E_PASSWORD=TestPassword123!

# Notes:
# - For local testing, use `npx supabase start` to get local Supabase credentials
# - Create a test user in your local Supabase instance with the credentials above
# - The OpenRouter API key is optional if you're not testing AI features
